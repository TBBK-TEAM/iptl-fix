org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true
# Do not silently download a JDK for the toolchain: CI provides JDK 21 via setup-java,
# and local builds are expected to run on a JDK 21 installation.
org.gradle.java.installations.auto-download=false
